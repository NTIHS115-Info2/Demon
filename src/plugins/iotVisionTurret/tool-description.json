{
  "toolName": "iotVisionTurret",
  "description": "Controls physical appliances in the IoT system. Use this tool whenever the user's intent involves switching a device on/off, changing the power state, or operating any supported home appliance (e.g., lights, fans, and other IR/IoT-controlled devices). Device selection and execution details are handled internally by the system; the tool returns only whether the operation succeeded.",
  "input": {
    "device": "String – Required. Target device type. Supported: 'light', 'fan'.",
    "method": "String – Required. Control method/action. Supported canonical values: 'turn_on', 'turn_off'. Synonyms are allowed (e.g., 'on', 'off', 'open', 'close', '開燈', '關燈')."
  },
  "inputExamples": {
    "lightOn": { "device": "light", "method": "turn_on" },
    "lightOff": { "device": "light", "method": "turn_off" },
    "fanOn": { "device": "fan", "method": "turn_on" },
    "fanOff": { "device": "fan", "method": "turn_off" }
  },
  "output": {
    "ok": "Boolean. true if the control operation succeeded, false if it failed."
  },
  "notes": [
    "The device must be registered and online before this tool can be used.",
    "IR codes are resolved via the IR_CODE_DICT mapping (device -> method -> code).",
    "The turret scans multiple positions and sends IR commands at each position to maximize coverage."
  ]
}
