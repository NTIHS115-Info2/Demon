use_default_settings: true

server:
  secret_key: "demon_scraper_fix_403"
  limiter: false       # 關閉限流器
  image_proxy: false
  bind_address: "0.0.0.0"
  port: 8080

search:
  formats:
    - html
    - json             # 必須顯式開啟 JSON，否則會報 403